export interface Options {
  dataMigration: boolean;
  onePassword: boolean;
  googleWorkspace: boolean;
  memoryUpgrade: boolean;
}

export interface CompanyInfo {
  companyName: string;
  contactName: string;
  email: string;
  phone: string;
}